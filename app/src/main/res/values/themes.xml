<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal pre-Compose window theme: only used for the brief cold-start window background
         and status bar color before setContent {} draws the real Material3 UI via
         InstaMapsTheme (see core:designsystem). -->
    <style name="Theme.InstaMaps" parent="android:Theme.Material.Light.NoActionBar" />
</resources>


