# Committed placeholder consumed by the secrets-gradle-plugin fallback (see app/build.gradle.kts).
# Create your own `secrets.properties` (gitignored, same directory) with a real key to run the app
# for real - this placeholder value only exists so a clean checkout still compiles.
#
# Get a key at https://console.cloud.google.com/google/maps-apis with the "Places API (New)" enabled.
PLACES_API_KEY=YOUR_API_KEY_HERE
